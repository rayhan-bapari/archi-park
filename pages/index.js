import Layout from "@/components/Layout/Layout";
import React from "react";

const Home = () => {
    return (
        <Layout>
            <div>Home</div>
        </Layout>
    );
};

export default Home;
